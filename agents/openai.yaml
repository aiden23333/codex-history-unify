interface:
  display_name: "Codex 会话历史统一"
  short_description: "切换 DeepSeek 与 GPT 后自动修复并恢复本地会话，无需手动操作"
  default_prompt: "Use $codex-history-unify to install the automatic switch guard or repair conversations after switching Codex providers."

policy:
  allow_implicit_invocation: true
